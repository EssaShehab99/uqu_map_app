import 'package:flutter/material.dart';

class AdminManageTimeSlotInformation extends StatelessWidget {
  const AdminManageTimeSlotInformation({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
