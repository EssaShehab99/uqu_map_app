import 'package:flutter/material.dart';

class AdminGenerateQR extends StatelessWidget {
  const AdminGenerateQR({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
